#include <iostream>
#include <vector>

using namespace std;
int main()
{
	int in;
	cin >> in;
	vector<int> data(in + 3);

	vector<int> dp(in + 3);

	for (int i = 1; i <= in; i++) cin >> data[i];

	for (int i = 1; i <= in; i++)
	{
		for (int j = 1)
		{

		}
	}
}