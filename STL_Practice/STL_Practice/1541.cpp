#include <iostream>
#include <string>

using namespace std;

int n;
char a[50];
int main()
{
	cin >> a;

	int m = 0;
	int count = 1;
	for (char i : a)
	{
		if (i == '+' || i == '-')
		{

		}
		else
			n = n * 10 + i = '0';

	}
}