#include <iostream>
#include <vector>
#include <algorithm>
#include <string>
#include <queue>
using namespace std;

int main()
{
	string a, b;
	cin >> a >> b;
	vector <char> x(a.size);
	vector <char> y(b.size);

	int count = 0;
	while (1)
	{
		
	}

}