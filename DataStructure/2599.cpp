#include <stdio.h>
#include <string.h>
#define N 10
int st[N][N]; //man , girl
int main()
{
	int max;
	scanf("%d", &max);2154
	for (int i = 1; i <= 3; i++)
		scanf("%d %d", &st, &st[i]);

	int check[N][N];
	memcpy(check, st, N);
	int i, j;

	for (i = 1; i <= 3; i++)
	{
		for (j = 1; j <= 3; j++)
		{
			if (i == j) continue; // 같은 자리는 안되
			게
		}
	}

}
