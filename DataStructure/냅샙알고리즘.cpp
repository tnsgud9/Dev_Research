#include <stdio.h>
#define N 10

int n;
int A[N], B[N];
int data[N][30];

int max(int a, int b) { return a > b ? a : b; }
int main() 
{
	int i, j,k;
	scanf("%d", &n);
	for (i = 1; i <= n; i++)
		scanf("%d %d", &A[i], &B[i]);

	for( k =0; k<b[i])

}